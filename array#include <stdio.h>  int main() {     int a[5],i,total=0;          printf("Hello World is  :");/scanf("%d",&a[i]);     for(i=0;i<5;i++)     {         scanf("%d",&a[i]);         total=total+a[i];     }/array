#include <stdio.h>

int main()
{
    int a[5],i,total=0;
    
    printf("Hello World is  :");
    //scanf("%d",&a[i]);
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
        total=total+a[i];
    }
    //printf("%d\n",a[i]);
    printf("the total is %d",total);

    return 0;
}
